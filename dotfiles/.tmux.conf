unbind C-b
unbind '"'
unbind %

set -g base-index 1
set -g pane-base-index 1
set -g mouse on
set -g prefix `

set-option -g allow-rename off

bind ` send-prefix
bind | split-window -h
bind - split-window -v
bind r source-file ~/.tmux.conf
